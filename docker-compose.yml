version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5174:5174"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    restart: unless-stopped
